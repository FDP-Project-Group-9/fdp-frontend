import {
    TypographyProps
} from '@mui/material';

interface StyledTypographyProps extends TypographyProps{
    color?: string;
    fontSize?: number
};

export type {
    StyledTypographyProps,
};